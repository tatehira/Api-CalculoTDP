namespace ProductsApi.Models
{
    public class Enums
    {
        public enum SSDType
        {
            Sata = 0,
            Nvme = 1,
        }

        public enum RotuloCategory
        {
            Upgrade = 0,
            Novo = 1,
        }

        public enum HDDType
        {
            HDDDesktop = 0,
            HDDNotebook = 0
        }
    }
}
